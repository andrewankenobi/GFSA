// Server configuration
const config = {
    // Change this URL to your server's URL
    serverUrl: 'http://localhost:5000'  // Default to localhost
};

// Export the configuration
window.appConfig = config; 